services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.v3
    container_name: linuxaio-v3
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    restart: unless-stopped
